{
  "name": "skip",
  "version": "0.3.0",
  "private": true,
  "description": "Honest reviews, not scams — paste a URL, good or bad, then buy",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18"
  }
}
